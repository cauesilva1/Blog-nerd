import { Button } from "../Button";

export default function PostButton() {
    return(
        <Button.Root href="#" className="mt-4">
        <Button.Content>ler</Button.Content>
        </Button.Root>
    )
}